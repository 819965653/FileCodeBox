version: "3"
services:
  file-code-box:
    image: filecodebox:test1
    volumes:
      - fcb-data:/app/data:rw
    restart: unless-stopped
    ports:
      - "2222:2222"
volumes:
  fcb-data:
    external: false